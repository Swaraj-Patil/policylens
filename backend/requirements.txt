# Web framework
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.10
pydantic-settings>=2.6
python-dotenv>=1.0

# LLM providers
#   - Ollama (local dev) is talked to over plain HTTP via httpx; no SDK required.
#   - Groq (production) uses the official SDK.
groq>=0.11
httpx>=0.28

# Embeddings — local, free, fast on Apple Silicon
sentence-transformers>=3.3

# Vector DB
chromadb>=0.5.23

# PDF parsing
pymupdf>=1.25

# Accessibility / readability scoring
textstat>=0.7

# CLI
typer>=0.15

# Dev
pytest>=8.3
